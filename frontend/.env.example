# Базовый URL Spring Boot API (без /api, без завершающего слэша).
# Локально не нужен — Vite прокси отправляет /api на localhost:8080.
# Продакшен (Vercel): URL вашего сервиса на Render, например:
# VITE_API_BASE_URL=https://branch-predictor-lab.onrender.com

VITE_API_BASE_URL=
